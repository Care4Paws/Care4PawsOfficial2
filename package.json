{
  "name": "care4paws",
  "version": "1.0.0",
  "description": "Care4Paws Official Project",
  "main": "index.js",
  "scripts": {
    "build": "echo 'Building project...'",
    "deploy": "npm cache clean --force && npm install && npm run build && npx wrangler publish"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "sqlite3": "^5.0.2",
    "bcrypt": "^5.0.1"
  }
}